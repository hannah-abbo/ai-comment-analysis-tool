# Required: OpenAI API key for GenAI theme classification
# Get your API key from: https://platform.openai.com/api-keys
# Without this key, the tool will use basic topic modeling without AI enhancement
OPENAI_API_KEY=sk-your_openai_api_key_here

# Optional: Server port (default: 3000)
PORT=3000

# Instructions:
# 1. Copy this file to .env
# 2. Replace sk-your_openai_api_key_here with your actual OpenAI API key
# 3. NEVER commit your .env file to GitHub (it's already in .gitignore)