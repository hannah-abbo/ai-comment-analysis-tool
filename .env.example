# Required: Anthropic Claude API key for GenAI theme classification
# Get your API key from: https://console.anthropic.com/settings/keys
# Without this key, the tool will use basic topic modeling without AI enhancement
ANTHROPIC_API_KEY=sk-ant-your_anthropic_api_key_here

# Optional: Server port (default: 3000)
PORT=3000

# Instructions:
# 1. Copy this file to .env
# 2. Replace sk-ant-your_anthropic_api_key_here with your actual Anthropic API key
# 3. NEVER commit your .env file to GitHub (it's already in .gitignore)